@page "/"
@layout MainLayout


<div class="main-body">

    <div class="view-header" >
        <div class="vh-left d-none d-md-block" >

        </div>
        <div class="vh-right" >
            <CustomButton Background="none" OnClick="OnTest"><Label>@SVGService.PlusIcon(30, 30)</Label></CustomButton>
        </div>
    </div>

    <div class="view-body">

    </div>

</div>

@code{
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if(firstRender){

           
        }
    }

    private async Task OnTest()
    {
        try
        {
            var dd = await D.Category.GetAsync();
            ComFn.JSConsole("reslt", dd);
        }
        catch (Exception ex)
        {
            ComFn.JSConsole("Exception", ex.Message);
        }
       
    }
}